package com.seed.model;

public enum USER_ROLE {

    ROLE_CUSTOMER,

    ROLE_RESTAURANT_OWNER,

    ROLE_ADMIN

}

//    In Java, enum is a special class used to define a fixed set of constants
//*                            // What is enum in Java? //
//  In Java, an enum (enumeration) is a special data type used to define a fixed set of constants.
//  It's useful when you have a group of related values that don’t change — like days of the week, directions, or user roles.*/ //
